class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        curr = head
        prev = None
        while curr:
            new = curr.next
            curr.next = prev
            prev = curr
            curr = new
        return prev
        
