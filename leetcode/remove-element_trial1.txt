class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        w, r, n = 0, 0, len(nums)
        while r < n:
            if nums[r] != val:
                nums[w] = nums[r]
                w += 1
            r += 1
        return w 